coverage:
  status:
    project:
      default:
        target: auto
        threshold: 2%
    patch:
      default:
        target: 80%
  ignore:
    - docs/**
    - src/lib/**
    - bundle-size/**
    - scripts/**
    - test/**
